{
	"query": "CREATE TABLE projects (id INTEGER PRIMARY KEY AUTOINCREMENT, name CHAR (200), description TEXT, added DATETIME, updated DATETIME, user_id INTEGER REFERENCES users (id) ON DELETE CASCADE);"
}
