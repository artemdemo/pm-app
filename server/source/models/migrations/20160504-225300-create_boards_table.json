{
    "query": "CREATE TABLE boards (id INTEGER PRIMARY KEY AUTOINCREMENT, name CHAR, description TEXT, id_position INTEGER, user_id INTEGER REFERENCES users (id) ON DELETE CASCADE, added DATETIME, updated DATETIME);"
}
