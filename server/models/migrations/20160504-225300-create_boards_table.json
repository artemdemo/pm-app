{
    "query": "CREATE TABLE boards (id INTEGER PRIMARY KEY AUTOINCREMENT, title CHAR, description TEXT, prev_column INTEGER, user_id INTEGER REFERENCES users (id) ON DELETE CASCADE, added DATETIME, updated DATETIME);"
}
